import { Popover, PopoverButton, PopoverPanel } from "@headlessui/react"
export default function ({ tierId }: { tierId: number }) {
  return (
    <Popover>
      <PopoverButton>ho</PopoverButton>
      <PopoverPanel>wow</PopoverPanel>
    </Popover>
  )
}
